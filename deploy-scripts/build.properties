#Wed Sep 30 20:05:34 PDT 2009

ant.lib.dir=lib

# Directory in which to dump compiled classes, WAR templates, etc.
local.build.dir=../build

# Directory to hold test coverage information
local.coverage.dir=${local.build.dir}/coverage
coverage.data.file=${local.coverage.dir}/cobertura.ser

# Directory where files specific to code coverage measurement & testing wil go.
local.test.dir=${local.build.dir}/test

# Top of web content tree.  Used as the root directory of WAR file.
local.web.dir=../WebContent

# Points to top of Java source tree.
local.src.dir=../src

# File to cache login info for outside services.
login.props.file=../login.properties

# Subversion information
svn.root=file:///Users/craser/Development/svnroot
svn.project.url=${svn.root}/KukidoBlogTemp
svn.work.dir=/Users/craser/Development/workspace/KukidoBlogTemp
svn.tags.url=${svn.root}/tags

# Info for connecting to production server, and depositing new WAR file.
prod.user=dreade2
prod.host=dreadedmonkeygod.net
prod.war.file=home.war
prod.war.dir=/public_html/webapps

# What to call local WAR file, where to get source files, put class files and lib, etc.
local.war.file=home.war
local.war.template=${local.build.dir}/war-template
# Used in classpath for Java compilation
local.lib.dir=${local.web.dir}/WEB-INF/lib

kukido.jar.file=kukido-blog.jar
coverage.jar.file=kukido-blog-cobertura.jar

# Testing JavaScript
javascript.src.dir=${local.web.dir}/javascript
javascript.tst.dir=javascript

# This is a total copout.  But it works for now. (2009/10/04)
resin.home.dir=/Users/craser/Development/Servers/resin-2.1.11
local.j2ee.lib.dir=${resin.home.dir}/lib
local.classes.dir=${local.build.dir}/classes

# Stuff for starting/stopping/deploying to local Resin instance.
resin.bin.dir=${resin.home.dir}/bin
resin.deploy.dir=${resin.home.dir}/webapps

